<!-- Fixed navbar -->
<header class="header">
  <nav class="p-3 navbar navbar-expand-lg navbar-light fixed-top bg-white">
    <div class="container">
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse"
        aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav">
          <li class="nav-item home 
            <% if (page === 'home') { %>
            active
            <% } %>
          ">
            <a class="nav-link" href="/">Inicio</a>
          </li>
          <% if (type === "staff") { %>
            <li class="nav-item
            <% if (page === 'create') { %>
            active
            <% } %>
          ">
              <a class="nav-link" href="/event/create">Crear evento</a>
            </li>
            <li class="nav-item 
            <% if (page === 'manage-events') { %>
            active
            <% } %>
          ">
              <a class="nav-link" href="/manage-events">Administrar eventos</a>
            </li>
            <% } %>
              <% if (type) { %>
                <li class="nav-item 
            <% if (page === 'booked-events') { %>
            active
            <% } %>
          ">
                  <a class="nav-link" href="/booked-events">Mis eventos</a>
                </li>
                <% } %>
        </ul>

        <!-- Search bar -->
        <form action="/search" class="form-inline ml-auto mr-2 mt-2 mt-lg-0">
          <div class="input-group">
            <input name="q" class="form-control form-control-sm" type="text" placeholder="Buscar" aria-label="Search">
            <div class="input-group-append">
              <button class="btn btn-sm search" type="submit">
                <i class="fas fa-search"></i>
              </button>
            </div>
          </div>
        </form>

        <% if (username) { %>
          <ul class="navbar-nav">
            <li class="nav-item dropdown mt-2 mt-lg-0">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true"
                aria-expanded="false">
                <%= username %>
              </a>
              <% const activityLink = `/user/${username}/activity`; %>
              <% const profileLink = `/user/${username}`; %>              
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="<%= profileLink %>">Perfil</a>
                  <a class="dropdown-item" href="<%= activityLink %>">Mi Actividad</a>
                  <a class="dropdown-item" href="/sign-out">Cerrar Sesion</a>
                </div>
            </li>
          </ul>
          <% } else { %>
            <ul class="navbar-nav">
              <li class="nav-item
                <% if (page === 'register') { %>
                active
                <% } %>
              ">
                <a class="nav-link ml-lg-2 mt-2 mt-lg-0" href="/register">Registro</a>
              </li>
              <li class="nav-item
                <% if (page === 'sign-in') { %>
                active
                <% } %>
              ">
                <a class="nav-link" href="/sign-in">Iniciar Sesion</a>
              </li>
            </ul>
            <% } %>
      </div>
    </div>
  </nav>
</header>