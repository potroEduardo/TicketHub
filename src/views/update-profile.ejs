<!DOCTYPE html>
<html lang="en">

<head>
  <title>Update
    <%= username %>'s Perfil - TicketHub</title>
  <%- include partials/html-head %>
</head>

<body class="checkout">

  <%- include partials/navbar %>

    <!-- Begin page content -->
    <main class="container">
      <div class="row">
        <div class="col-sm-10 col-md-8 col-lg-4">
          <header class="title mt-3 mb-2">Actualizar
            <%= username %> Perfil</header>
          <form autocomplete="off" data-username="<%= username %>">
            <div class="form-group">
              <label for="email" class="sr-only">Email</label>
              <input type="email" class="form-control" id="email" name="email" placeholder="Email" value="<%= email %>" required>
            </div>

            <div class="form-group">
              <label for="password" class="sr-only">Nueva Contraseña</label>
              <input type="password" name="password" class="form-control" id="password" placeholder="Nueva Contraseña">
              <small class="form-text text-muted">
                Dejar vacio si la constraseña no se modifica.
              </small>
            </div>

            <div class="form-group">
              <label for="confPassword" class="sr-only">Confirmar Contraseña</label>
              <input type="password" name="confPassword" class="form-control" id="confPassword" placeholder="Confirm password">
            </div>
            <button type="submit" class="btn btn-primary btn-update">Actualizar</button>
          </form>
        </div>
      </div>

      <!-- Modal -->
      <div id="user-modal" class="modal" tabindex="-1" role="dialog">
        <div class="modal-dialog modal-dialog-centered" role="document">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title text-danger">Actualizacion fallidas</h5>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
            <div class="modal-body">
              <p></p>
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Cerrar</button>
            </div>
          </div>
        </div>
      </div>
    </main>

    <%- include partials/footer %>

      <!-- Bootstrap core JavaScript
    ================================================== -->
      <!-- Placed at the end of the document so the pages load faster -->

      <%- include partials/html-scripts %>
        <script src="/assets/update-profile.js"></script>
</body>

</html>